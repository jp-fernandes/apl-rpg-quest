import { MaxTwoDigitsDirectiveDirective } from './max-two-digits-directive.directive';

describe('MaxTwoDigitsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxTwoDigitsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
